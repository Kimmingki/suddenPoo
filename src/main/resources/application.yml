spring:
  config:
    import: optional:file:.env[.properties]

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

logging.level:
  org.hibernate.sql: debug

map:
  id: ${MAPID}